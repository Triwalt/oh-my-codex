interface:
  display_name: "Autopilot"
  short_description: "Autonomous build with serial-first reliability"
  brand_color: "#22C55E"

dependencies:
  tools:
    - type: "mcp"
      value: "claude_code"
      description: "Claude Code for implementation and verification loops"
